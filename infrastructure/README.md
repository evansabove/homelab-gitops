# infrastructure

Cluster-level services (ingress controller, cert-manager, storage,
monitoring, …). Each service gets its own subdirectory here plus an
`Application` manifest in `apps/` pointing at it.
